<div class="header row">
  <div class="col-md-6">
    <h2><%= @title %></h2>
  </div>
</div>

<div id="addons">
  <div class="well">Loading...</div>
</div>

<script id="tmpl-addon" type="text/x-handlebars-template">
  {{#each addons}}
    <div class="addon row" data-name="{{ name }}" data-endpoint="{{ endpoint }}">
      <div class="col-sm-10">
        <h2>{{ name }} <small>{{ latest.number }}</small></h2>
        <p class="lead">{{ latest.description }}</p>
      </div>
      <div class="col-sm-2">
        <a href="/addons/install" class="btn btn-primary install">
          Install
        </a>
      </div>
    </div>
    <hr>
  {{/each}}
</script>
